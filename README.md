# pi-clock
Use raspi-config to 
* Change password
* Set hostname
* Connect to wifi
* Set **boot to console** instead of desktop
* Enable spi and i2c

clone to /home/pi, cd to pi-clock and run **sudo sh setup.txt**
